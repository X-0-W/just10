<div id="message-<%= message.id %>">
  <small>
    <strong><%= message.user.first_name %></strong>
    <i style="float: right;"><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p class="mb-2"><%= message.content %></p>
</div>
<%# delete once its been marged  %>
